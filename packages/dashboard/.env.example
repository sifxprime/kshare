# Admin API — Nginx proxies admin.YOUR_DOMAIN/api/admin/ → localhost:4001
# Set this to your actual admin subdomain
NEXT_PUBLIC_ADMIN_API=https://admin.yourdomain.com/api/admin

# Public API origin — used by the tunnel status page
NEXT_PUBLIC_API_ORIGIN=https://api.yourdomain.com
